// full-screen photo banner, same pattern as PartnershipHero/TechnologyHero
// — ContactHero (the next section) wraps up over its bottom edge via a
// scroll-tracked margin, with the "CONTACT" folder-notch tab riding up
// together with it.
export default function ContactHeroPhoto() {
  return (
    <section className="contact-hero-photo" id="home">
      <div className="contact-hero-photo-overlay" />
    </section>
  );
}
